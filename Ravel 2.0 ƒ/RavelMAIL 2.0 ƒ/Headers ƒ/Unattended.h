typedef struct _manual_outbound_c {	struct _manual_outbound_c	*next;		addr				calladdr;	short				poll;	Str31				callphone;	char				password[8];	short				force;	short				tempstop;	short				badcount;		Handle				requests;	Handle				attaches;} manual_chainType, *manual_chainPtr;typedef struct _tobesend {	long				pkt;	long				other;} tsend;typedef struct _in_outbound {	struct _in_outbound	*next;	//	¥	dynamical outbound params & from node description	addr				calladdr;	short				useaka;	short				poll;	Str31				callphone;	char				password[8];	tsend				tobesend;	short				tempstop;	short				badcount;		long				evt_mask;	_period				nodelist_time;	hiddenlinesPtr		*hiddens;	StringPtr			inits;	//	¥	manual outbound params	Boolean				force;	Handle				requests;	Handle				attaches;} in_outboundType, *in_outboundPtr;void	ClearOutboundStruct ( void );void	AddOutbound ( short, StringPtr, unsigned char, short, short, short, short );void	RescanOutbound ( void );void	Unattended ( void );Boolean	CheckService ( void );